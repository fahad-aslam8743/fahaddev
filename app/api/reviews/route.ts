import {NextResponse} from 'next/server';
import {supabaseAdmin} from '@/lib/supabase';
export async function GET(){const sb=supabaseAdmin();if(!sb)return NextResponse.json([]);const {data,error}=await sb.from('reviews').select('*').order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(data)}
export async function POST(req:Request){const sb=supabaseAdmin();if(!sb)return NextResponse.json({error:'Reviews database is not configured yet.'},{status:503});const body=await req.json();if(body.website)return NextResponse.json({ok:true});const name=String(body.name||'').trim().slice(0,80),message=String(body.message||'').trim().slice(0,800),role=String(body.role||'').trim().slice(0,100),company=String(body.company||'').trim().slice(0,100),rating=Math.max(1,Math.min(5,Number(body.rating)||5));if(name.length<2||message.length<12)return NextResponse.json({error:'Please add your name and a little more detail.'},{status:400});const {data,error}=await sb.from('reviews').insert({name,message,role:role||null,company:company||null,rating}).select().single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json(data,{status:201})}
