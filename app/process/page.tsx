import type {Metadata} from 'next'; import Link from 'next/link';
export const metadata:Metadata={title:'Process',description:'A clear project process from discovery and scope through build, launch and handoff.'};
const steps=[['Discovery','Week 1','We talk through the business goal, users, existing constraints and what success should look like. You leave with sharper requirements—not a vague “we can build anything” promise.'],['Direction & scope','Week 1','I turn the discussion into a written scope: deliverables, assumptions, exclusions, price and expected timeline. That document becomes the shared reference for the build.'],['Design & structure','Week 1–2','Key journeys and screens are shaped before deep implementation. You approve the direction while changes are still cheap to make.'],['Build & previews','Week 2–4+','The product is implemented in stages. You see progress at agreed checkpoints, with feedback handled while the context is still fresh.'],['QA, launch & handoff','Launch','Responsive checks, production build, deployment, domain connection and a handoff of the accounts and access your business needs to own.'],['Support','Post-launch','An agreed support window covers launch-related bugs. Ongoing improvements can continue separately if the product needs them.']];
export default function Process(){return <section className="page shell"><div className="page-intro"><span className="eyebrow">How we work together</span><h1>No black box between kickoff and launch.</h1><p>The process is designed to reduce the two things clients dislike most: uncertainty and surprises.</p></div><div className="timeline">{steps.map((s,i)=><article key={s[0]}><span>0{i+1}</span><div><small>{s[1]}</small><h2>{s[0]}</h2><p>{s[2]}</p></div></article>)}</div><div className="soft-cta"><h2>No surprise invoices. No silent scope creep.</h2><p>If the project changes, the impact is discussed before the work changes with it.</p><Link className="btn" href="/contact">Start with a short call</Link></div></section>}
