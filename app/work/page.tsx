import type {Metadata} from 'next'; import {projects} from '@/lib/projects'; import {ProjectCard} from '@/components/ProjectCard';
export const metadata:Metadata={title:'Work — Fahad',description:'A mix of real client builds and labeled concept projects — built to the same standard either way.'};
export default function Work(){return <section className="page shell"><div className="page-intro"><span className="eyebrow">Work</span><h1>Selected Work</h1><p>A mix of real client builds and labeled concept projects — built to the same standard either way.</p></div><div className="project-grid">{projects.map(p=><ProjectCard key={p.slug} p={p}/>)}</div></section>}
