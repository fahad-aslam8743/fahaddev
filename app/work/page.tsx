import type {Metadata} from 'next'; import {projects} from '@/lib/projects'; import {ProjectCard} from '@/components/ProjectCard';
export const metadata:Metadata={title:'Work',description:'Case studies of full-stack Next.js builds — real projects, clearly labeled concepts, and the problems behind each one.'};
export default function Work(){return <section className="page shell"><div className="page-intro"><span className="eyebrow">Selected work</span><h1>Built around the problem, not the template.</h1><p>Commerce, dashboards and full-stack web products—shown with the reasoning behind the interface, not just the final screenshot.</p></div><div className="project-grid">{projects.map(p=><ProjectCard key={p.slug} p={p}/>)}</div></section>}
