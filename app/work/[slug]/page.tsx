import {notFound} from 'next/navigation'; import Link from 'next/link'; import {ArrowUpRight} from 'lucide-react'; import {getProject,projects} from '@/lib/projects';
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export default async function CaseStudy({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=getProject(slug);if(!p)return notFound();return <article className="case shell"><div className="case-head">{p.concept&&<span className="concept">Concept Build</span>}<h1>{p.title}</h1><p>{p.short}</p><div className="case-meta"><span>{p.clientType}</span><span>{p.timeline}</span><span>{p.stack.join(' · ')}</span></div></div><div className={`case-visual visual-${p.slug}`}><div className="browser"><span/><span/><span/></div><div className="mock-copy"><small>{p.clientType}</small><strong>{p.title}</strong><em>Product interface study</em></div></div><div className="case-content"><section><span>01</span><div><h2>The problem</h2><p>{p.problem}</p></div></section><section><span>02</span><div><h2>The approach</h2><p>{p.approach}</p></div></section><section><span>03</span><div><h2>The build</h2><p>{p.build}</p></div></section><section><span>04</span><div><h2>The result</h2><p>{p.result}</p></div></section></div>{p.liveUrl&&<a className="btn" href={p.liveUrl}>Visit live site <ArrowUpRight size={17}/></a>}<div className="case-cta"><h2>Need something with the same level of ownership?</h2><Link className="btn" href="/contact">Talk about your project</Link></div></article>}
