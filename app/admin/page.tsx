export default async function Admin({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <section className="admin-login"><form action="/api/admin/login" method="post"><div className="wordmark">fahaddev</div><h1>Private admin</h1><p>Project leads, portfolio entries and moderated reviews.</p>{error&&<p className="admin-error" role="alert">That password was not accepted.</p>}<label>Password<input type="password" name="password" required autoComplete="current-password"/></label><button className="btn" type="submit">Continue</button></form></section>}
