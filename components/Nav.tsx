'use client';
import Link from 'next/link';
import { Menu, X, ArrowUpRight } from 'lucide-react';
import { useEffect, useState } from 'react';
const links=[['Work','/work'],['Services','/services'],['Process','/process'],['About','/about']] as const;
export function Nav(){const [open,setOpen]=useState(false);useEffect(()=>{const root=document.documentElement;root.classList.toggle('menu-open',open);return()=>root.classList.remove('menu-open')},[open]);return <header className="nav-wrap"><nav className="nav shell" aria-label="Primary navigation"><Link className="wordmark" href="/" onClick={()=>setOpen(false)}>fahaddev<span>.</span></Link><div className="nav-links"><span className="nav-status"><i/>Available for new projects</span>{links.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}<Link className="btn btn-sm" href="/contact">Free consultation</Link></div><button className="menu-btn" type="button" aria-expanded={open} aria-controls="mobile-navigation" aria-label={open?'Close navigation':'Open navigation'} onClick={()=>setOpen(v=>!v)}>{open?<X size={21}/>:<Menu size={21}/>}</button></nav><div id="mobile-navigation" className={`mobile-menu ${open?'is-open':''}`} aria-hidden={!open}><div className="mobile-menu-inner shell"><div className="mobile-menu-top"><span className="nav-status"><i/>Available for new projects</span><span>MENU</span></div><div className="mobile-menu-links">{links.map(([n,h],i)=><Link key={h} href={h} onClick={()=>setOpen(false)}><small>0{i+1}</small><b>{n}</b><ArrowUpRight size={19}/></Link>)}</div><div className="mobile-menu-bottom"><p>Have something to build, improve, or rescue?</p><Link className="btn" href="/contact" onClick={()=>setOpen(false)}>Book a Free Project Consultation <ArrowUpRight size={17}/></Link><Link className="mobile-email" href="mailto:hello@fahaddev.com">hello@fahaddev.com</Link></div></div></div></header>}
