import { Star, MessageSquareQuote } from 'lucide-react';
import { getReviews } from '@/lib/reviews';
import { ReviewForm } from './ReviewForm';
import { Reveal } from './Reveal';
export async function Testimonials(){const reviews=await getReviews();return <section className="section testimonial-section"><div className="shell"><Reveal className="section-head centered"><span className="eyebrow">Client feedback</span><h2>Proof should come from real project experience, not invented numbers.</h2><p>Published reviews appear here exactly because trust is more useful when it is earned and specific.</p></Reveal><Reveal className="testimonial-layout"><div className="review-grid">{reviews.length?reviews.map(r=><article className="review-card" key={r.id}><div className="stars">{Array.from({length:r.rating||5}).map((_,i)=><Star key={i} size={15} fill="currentColor"/>)}</div><blockquote>“{r.message}”</blockquote><footer><b>{r.name}</b><span>{[r.role,r.company].filter(Boolean).join(' · ')}</span></footer></article>):<article className="review-card review-placeholder"><MessageSquareQuote/><h3>No placeholder testimonials.</h3><p>Verified project feedback will appear here as clients submit it. Until then, the case studies carry the proof.</p></article>}</div><ReviewForm/></Reveal></div></section>}
