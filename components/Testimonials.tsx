import { Star, MessageSquareQuote } from 'lucide-react';
import { getReviews } from '@/lib/reviews';
import { ReviewForm } from './ReviewForm';
import { Reveal } from './Reveal';
export async function Testimonials(){const reviews=await getReviews();return <section className="section testimonial-section"><div className="shell"><Reveal className="section-head"><span className="eyebrow">Client feedback</span><h2>Trust is stronger when the experience is described by the people who had it.</h2><p>Read feedback or share your own experience with a finished project or collaboration.</p></Reveal><Reveal className="testimonial-layout"><div className="review-grid">{reviews.length?reviews.map(r=><article className="review-card" key={r.id}><div className="stars">{Array.from({length:r.rating||5}).map((_,i)=><Star key={i} size={15} fill="currentColor"/>)}</div><blockquote>“{r.message}”</blockquote><footer><b>{r.name}</b><span>{[r.role,r.company].filter(Boolean).join(' · ')}</span></footer></article>):<article className="review-card review-placeholder"><MessageSquareQuote/><h3>Client feedback will appear here.</h3><p>The review system is ready for verified project feedback as work is completed.</p></article>}</div><ReviewForm/></Reveal></div></section>}
