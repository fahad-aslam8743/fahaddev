import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import {Project} from '@/lib/projects';
export function ProjectCard({p}:{p:Project}){return <article className="project-card"><div className={`project-visual visual-${p.slug}`}><div className="visual-grid"/><div className="browser-bar"><i/><i/><i/><span>{p.slug}.project</span></div><div className="project-screen"><small>{p.clientType}</small><strong>{p.title}</strong><div>{p.stack.slice(0,3).map(x=><span key={x}>{x}</span>)}</div></div></div><div className="project-body"><div className="project-kicker">{p.concept&&<span className="concept">Concept</span>}<span>{p.clientType}</span></div><h3>{p.title}</h3><p>{p.short}</p><Link className="case-button" href={`/work/${p.slug}`}>Open case study <ArrowUpRight size={16}/></Link></div></article>}
