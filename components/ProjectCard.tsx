import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import { Project } from '@/lib/projects';
export function ProjectCard({p}:{p:Project}){return <article className="project-card"><div className={`project-visual visual-${p.slug}`}><div className="browser"><span/><span/><span/></div><div className="mock-copy"><small>{p.clientType}</small><strong>{p.title}</strong><em>Interface preview</em></div></div><div className="project-body"><div className="project-kicker">{p.concept&&<span className="concept">Concept Build</span>}{p.stack.slice(0,3).map(s=><span key={s}>{s}</span>)}</div><h3>{p.title}</h3><p>{p.short}</p><Link className="text-link" href={`/work/${p.slug}`}>View case study <ArrowUpRight size={16}/></Link></div></article>}
